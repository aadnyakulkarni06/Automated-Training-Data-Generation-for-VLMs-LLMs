from typing import List


def generate_image_captions(image_paths: List[str]) -> List[str]:
    """Placeholder caption generator.

    Replace with a model-based captioner (e.g., BLIP/CLIP+LLM).
    """
    captions = [f"Caption for {p}" for p in image_paths]
    return captions
